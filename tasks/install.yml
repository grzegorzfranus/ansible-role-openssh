---
# =============================================================================
# Ansible Role: OpenSSH - Tasks: Install
# =============================================================================
# Install OpenSSH package dependencies.
# =============================================================================
- name: OpenSSH | install | Install OpenSSH packages
  become: true
  ansible.builtin.package:
    name: "{{ __openssh_packages }}"
    state: present
